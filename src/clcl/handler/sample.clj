;; (ns clcl.handler.sample
;; (:require [clcl.event :as event])
;; )

;; For hook, we need to implement `event-name' and `run'

;; (def event-name event/issue-comment)

;; (defn run [payload config]
;;   (cool-fn payload config))
